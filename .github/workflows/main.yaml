name: sample container web
on:
  workflow_dispatch:
    inputs:
      AWS_DEPLOY_ENVIRONMENT:
        type: choice
        description: 'Deployment Environment'
        required: true
        options:
        - DEV
        - QA
        - STAGE
        - TEST
        - PROD
